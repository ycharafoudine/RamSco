{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, { 'action': /profile/editer, 'attr': { 'class': 'fos_user_profile_edit' } }) }}
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'profile.edit.submit'|trans }}" />
    </div>
{{ form_end(form) }}
